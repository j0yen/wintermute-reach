/target
!/target/autobuilder/receipts/
!/target/autobuilder/results.tsv
!/target/autobuilder/postmortem.md
**/*.rs.bk
Cargo.lock.bk
.DS_Store
# subagent isolation worktrees (Claude Code Agent --isolation=worktree
# creates these inside the slice; never part of the slice artifact)
.claude/worktrees/
