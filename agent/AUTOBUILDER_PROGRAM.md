# wintermute-reach — autobuilder program

Generated by autobuilder Stage 2. Read in full before every iteration.

## What you are doing

You are the inner-loop coder for `wintermute-reach`, a Rust CLI project being built
under autobuilder's iterate-and-prove discipline. Your contract is `agent/intent-card.json`.
Your job is to propose one diff per iteration; the outer loop runs the gates and decides
advance or revert.

## Files

| Path | Read | Write |
|---|---|---|
| `src/**` | ✓ | ✓ |
| `agent/intent-card.json` | ✓ | ✗ (write `intent_card_amendment_request.json` instead) |
| `agent/AUTOBUILDER_PROGRAM.md` (this file) | ✓ | ✗ |
| `agent/owner-map.json`, `agent/test-map.json`, `agent/proof-lanes.toml` | ✓ | ✗ |
| `tests/acceptance_*.rs`, `tests/proptest_invariants.rs` | ✓ | ✗ |
| `scripts/run-metrics.sh`, `scripts/audit.sh`, `scripts/risk-gate.sh` | ✓ | ✗ |
| `Cargo.toml` (full) | ✓ | only via deps-only iteration with `description` prefix `deps:` |
| `Cargo.lock` | ✓ | regenerated by cargo on deps iterations |
| `clippy.toml`, `deny.toml`, `rust-toolchain.toml` | ✓ | ✗ |
| `.github/workflows/**` | ✓ | ✗ |
| `target/autobuilder/**` | ✓ | written by orchestrator only |
| `dist/wm-reach.service` | ✓ | ✗ |

If you need to modify a `✗` file to make progress, **stop** and write
`agent/intent_card_amendment_request.json`.

## The unfakeable metric

```
name:             acceptance_tests_passing_count
lower_is_better:  false
harness:          scripts/run-metrics.sh
```

## Hard gates (all must pass to advance)

1. `cargo check --workspace`
2. `cargo clippy --workspace -- -D warnings`
3. `cargo test --workspace`
4. `cargo deny check`
5. BAD_RUST audit (`scripts/audit.sh`) — zero `BLOCKING` findings
6. Proof-lane routing — every changed path resolves to ≥1 lane in `agent/proof-lanes.toml`

## Domain notes for edit-agent

- The agorabus dep is a path dep at `/home/jsy/wintermute/agorabus`.
- AC2/AC3/AC5 tests use a fake sendmail script via `WM_REACH_SENDMAIL` env var.
- AC8 tests the self-emitted filter: `event.from == own_session_id`.
- AC9 test checks `dist/wm-reach.service` ExecStart path — `.local/bin/wm-reach`.
- Transport errors must NEVER panic; encode as `DeliveryResult { delivered: false, error: Some(...) }`.
- No credentials in Debug output, logs, or panics.

## Things to never do

- Don't add `clone()`, `Arc<Mutex<_>>`, `Box::leak`, `Rc<RefCell<_>>`, `static mut`,
  `unsafe`, or `transmute` to satisfy the borrow checker.
- Don't `unwrap()` / `expect()` on user/network/file/env/CLI input. Use `?`.
- Don't add a `todo!()`, `unimplemented!()`, `panic!("should not happen")`, or
  placeholder branch in a reachable path.
- Don't log credentials, tokens, or passwords.
- Don't add a dependency in the same iteration as a behavior change.
